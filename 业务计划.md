# MC 拼豆图纸生成工具 —— 业务计划

> 版本: v0.2  ·  日期: 2026-08-04  ·  目标版本: Minecraft Java Edition 26.2

## 1. 项目概述

将 Minecraft 中的内容转化为**拼豆图纸**（Perler Beads / 拼拼豆豆）：图纸中每颗豆 = 一个像素单元，玩家照着图纸用对应颜色的豆子拼出 MC 的物品、方块或场景。

项目包含三大子系统：

1. **PatternTool（离线 Python 脚本）** — 输入原版纹理 PNG，直接生成图纸，无需启动游戏；
2. **MC 模组（游戏内）** — 在游戏内采样世界数据（截面 / 视角截图 / 墙面视图），生成图纸；
3. **Web 平台（本地应用）** — 契约 JSON / 图片导入、从零新建、绘制编辑、IndexedDB 持久化（Vue 3）。

## 2. 功能需求

### F1 单物品 / 方块贴图 → 图纸（Python 脚本，离线）

- 输入：纹理 PNG（`assets/minecraft/textures/block/*.png`、`item/*.png`）；
- **方块多面**：一个方块自动输出其各面（如 工作台 front / side / top）的图纸，可按需选择面；
- 选项：
  - 豆渲染尺寸（每颗豆的像素大小，默认 20px）；
  - 网格线开关（拼豆计数必需）；
  - 颜色图例（每种使用到的颜色输出色块 + HEX 色号，便于购买对应颜色豆子）；
  - 调色板映射（可选，后续迭代）：将颜色归约到指定拼豆品牌色卡（Perler / Hama / Artkal）；
  - 透明像素处理（默认填充白色背景，模拟实体豆盘）；
  - 批量处理（后续迭代）：一个文件夹/一份清单 → 一批图纸。

### F2 游戏内二维采样 → 图纸（模组，指令驱动）

统一指令语法（两对角坐标定义轴对齐平面，必须共面）：

```
/perler view <x1> <y1> <z1> <x2> <y2> <z2> --direction <east|south|west|north|up|down> [选项]
/perler section <x1> <y1> <z1> <x2> <y2> <z2> [选项]     # 取平面本身那一层
```

- **正交视图（view，核心）**：从平面出发、沿 `--direction` 方向逐格探测，取**第一个有效方块**作为该格像素（正交投影/光线投射，每个格子 1 格 = 1 像素）；
- **截面（section）**：取平面本身上所有方块（地层剖面等场景），与 view 共享平面解析器；
- 方向合法性按平面法线校验：竖直面仅 东/西 或 南/北 有效，水平面仅 上/下 有效，非法方向拒绝并提示；
- 选项：
  - `--distance N` 探测最大距离（默认 16，**硬上限 256**，防卡死）；
  - **忽略集**：默认忽略 `minecraft:air` / `minecraft:barrier`（屏障）/ `minecraft:structure_void`（结构空位）三类；`--ignore <方块id>` 追加忽略项，`--keep <方块id>` 从忽略集移除（如想看空气层就 `--keep minecraft:air`）；
  - `--granularity 1|2|4|8|16` **聚合粒度**：1 颗豆 = 方块**面纹理内 g×g 像素**的平均色（g=16 即整面平均 = 默认 1方块1豆；g=1 纹理原样 1像素1豆，每方块最多 16×16 豆，最精细；g 越大图纸越小）；
  - `--bg <rgb>` 背景色、`--scale N` 单格大小、`--palette <品牌>` 豆号色卡、`--save <路径>` 输出位置、`--json` 导出契约 JSON；
- 命令**异步执行**（独立线程池），完成发回执，避免卡服；
- **取色（M5 已完成）**：优先取**被看到的那一面**的**真实纹理像素**——客户端命令版解析 assets 模型文件（blockstates/models JSON + 贴图）得到 16×16 面纹理，按 granularity 聚合；服务端命令版保留 MapColor 平均色（无资源环境回退）；
- 面纹理取色已知限制：忽略 blockstates variant 旋转、忽略 tintindex（草/树叶偏原纹理色）、多 element 模型近似采样。

### F3 玩家视角截图 → 图纸（模组，客户端命令）

- **F9 快捷键**（默认）：画面干净无输入框/聊天栏；参数由 `/perler screenshot config` 配置（长边/名称前缀/保存路径，持久化配置文件）；
- `/perler screenshot --long-edge N`：**长边豆数**（如 64），短边按截图宽高比自动计算（如 16:9 → 64×36 豆）；
- 读帧缓冲（复用官方截图管线）→ **平均降采样**到目标豆数 → 颜色网格 → 契约 JSON（palette 按豆号聚合 + bead_rgb）；
- 与 view 的差异：截图是透视 + 光照着色效果，view 是纯数据采样（无光照）——两种风格并存。

### F3.5 Web 平台（本地图纸编辑器）

- 导入：契约 JSON（文件/拖拽/粘贴）、**图片**（长边像素数降采样自动生成图纸）；
- 编辑：画笔/橡皮/填充/取色/矩形/直线、缩放平移、撤销重做、色板（图纸颜色 + MARD 全色卡 tab）；
- 持久化：IndexedDB，**手动保存**（blur 遮罩 + 保存动画），未保存红点指示；
- 导出：契约 JSON（含 bgColor）、PNG；
- 性能：离屏 canvas + 脏矩形局部重绘、grid 非响应式（markRaw + Uint16Array）、rAF 批量、历史差异存储。

### F4 二维墙面视图（并入 F2 的 view）

竖直面 + 对应水平方向（东/西 或 南/北）就是墙面视图，不再单独成功能——如东面看墙：`/perler view ... --direction east`。

## 3. 目标用户与使用场景

| 用户 | 场景 |
|------|------|
| 拼豆手工爱好者 | 拼一个小物品（16×16 豆 ≈ 8cm）或一面墙（大图） |
| 建筑玩家 | 把 MC 里的建筑截面、像素画墙打印成施工图 |
| 内容创作者 | 批量生成道具/物品图纸分享 |

## 4. 里程碑

| 里程碑 | 内容 | 状态 |
|--------|------|------|
| **M1-M2** | Python 工具：批量转换（4378 张）、MARD 色卡、动画拆分、3 种颜色模式 | ✅ |
| **M3** | 模组：section/view 命令树、豆号输出（MardPalette）、26.2 生态排障 | ✅ |
| **M4** | F3 玩家视角截图：命令 + F9 快捷键 + 配置命令 | ✅ |
| **M5** | 面纹理取色：FaceTextureProvider（自解析模型）+ granularity 真聚合 | ✅ |
| **M6** | Web 平台：导入/新建/编辑/持久化/性能优化 | ✅ |
| **M7** | 3D 实体视图 / 立体拼豆展开（渲染管线 + 掩码图纸） | 规划中 |

## 5. 风险与注意事项

| 风险 | 应对 |
|------|------|
| 26.2 生态差异（无混淆/无 mappings/brigadier 1.3.10 立即 build） | 已全部排障并记录在技术架构.md 第 8 节（构建经验） |
| 原版贴图版权归 Mojang，图纸仅限个人手工使用 | 工具不内置任何资源文件，只处理用户自备的 assets；README 含版权声明 |
| 颜色匹配精度受显示器色域、拼豆品牌色差影响 | CIE Lab 最近邻 + 色差阈值提示（`!` 前缀）；Python 与模组端算法交叉验证一致 |
| 大区域采样（墙面）性能开销 | 硬上限（面 ≤512×512、距离 ≤256、豆数 ≤1M）+ 异步线程池 |
| Web 平台大图纸编辑性能 | 离屏 canvas + 脏矩形、grid 非响应式、rAF 批量、历史差异存储（M6 已实施） |

## 6. 范围之外（暂不做）

- 3D 立体模型展开（如 3D 体素模型 → 多面展开图）；
- 非轴对齐平面 / 曲面采样；
- 直接打印 PDF 排版（M2 视需求再加）。
