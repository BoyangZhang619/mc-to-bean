# 技术架构

> 版本: v0.3  ·  日期: 2026-08-04  ·  目标版本: Minecraft Java Edition 26.2

## 1. 总体架构

三大子系统，共享同一种**图纸数据格式**（契约 JSON），渲染/编辑/导出逻辑各自复用：

```
┌─────────────────────┐   ┌──────────────────────┐
│  PatternTool        │   │  MC 模组 (Fabric)    │
│  (Python, 离线)     │   │  (游戏内, 世界采集)  │
│ 纹理PNG ──► 网格     │   │ 截面/视图/截图 ──► JSON │
│ 渲染导出 ◄── 契约JSON │   │ 面纹理取色(M5)       │
└──────────┬──────────┘   └──────────┬───────────┘
           │         ┌───────────────┘
           ▼         ▼
   ┌───────────────────────────────┐
   │  Web 平台 (Vue3, 本地)        │
   │  JSON/图片导入 → 编辑 → IndexedDB │
   │  → 契约 JSON / PNG 导出        │
   └───────────────────────────────┘
```

- **PatternTool**：不依赖游戏，直接处理 `assets/` 下的纹理，批量产出图纸与契约 JSON；
- **MC 模组**：游戏内采样（正交视图/截面/截图），面纹理取色，产出契约 JSON；
- **Web 平台**：图纸的查看/编辑/管理中枢，IndexedDB 本地持久化，三端互通。

## 2. 目录结构

```
mc-to-bean/
├── 业务计划.md / 技术架构.md / README.md
├── assets/                      # 官方原版资源（用户自备，不入库）
├── pattern-tool/                # 【子系统 A】Python 离线工具
│   ├── entry.py                 #   交互式入口（含 JSON 导出）
│   ├── texture_to_pattern.py    #   纹理 → 图纸 + 契约 JSON（CLI）
│   ├── json_to_pattern.py       #   契约 JSON → 图纸 PNG（含 bead_rgb 豆号色）
│   ├── batch_convert.py         #   全量批量转换（并行/断点续跑）
│   ├── split_animations.py      #   动画纹理逐帧拆分
│   ├── palette.py / render.py   #   色卡匹配（Lab 最近邻）/ 渲染核心
│   └── data/                    #   色卡 CSV（MARD 291 色等）
├── mod/                         # 【子系统 B】Fabric 模组（26.2）
│   └── src/main/java/dev/mcperler/
│       ├── command/             #   PerlerCommand(服务端) / ClientPerlerCommand(客户端面纹理版)
│       │                        #   ClientScreenshotCommand / ScreenshotHotkey(F9) / 配置命令
│       ├── section/             #   SectionPlane / ViewSampler(granularity 真聚合) / SampleOptions
│       ├── color/               #   BlockColorMapper / FaceTextureProvider(自解析模型)
│       ├── export/              #   PatternExporter(豆号聚合 + bead_rgb)
│       └── palette/             #   MardPalette(Java 版 Lab 匹配)
└── web-app/                     # 【子系统 C】Web 平台（Vue3 + Pinia + IndexedDB）
    └── src/                     #   views(4路由) / components / stores / composables / utils
```

## 3. 图纸数据格式（共享 JSON）

模组与脚本互通的核心契约：

```json
{
  "name": "crafting_table_front",
  "width": 16,
  "height": 16,
  "cell_size_mm": 5,
  "palette": [
    { "index": 0, "rgb": [140, 116, 84], "name": null },
    { "index": 1, "rgb": [104, 84, 62],  "name": null }
  ],
  "grid": [
    [0, 1, 1, 0, 0, ...],
    [...]
  ]
}
```

- `grid[y][x]` 为调色板索引，紧凑且便于任何语言读取；
- `palette[].name` 用于调色板匹配后填品牌色号（如 Perler P01），未匹配时置 `null`；
- `cell_size_mm` 记录目标豆直径（默认 5mm，拼豆常见规格），用于未来 PDF 按实际尺寸排版。

## 4. 子系统 A：PatternTool（Python）

### 4.1 渲染管线

```
纹理 PNG ──► 透明处理 ──► (可选) 调色板最近色匹配 ──► 网格构建
      ──► Pillow 渲染（网格线 / 轴编号 / 图例）──► 图纸 PNG
      ──► (可选) 图纸 JSON 导出
```

### 4.2 库选型（用户关心点：好看的图纸生成）

| 库 | 定位 | 结论 |
|----|------|------|
| **Pillow** | 像素级图像渲染 | ✅ **主渲染库**。图纸本质是"格子图"，需要 1px 级控制（网格线、色块、文字），Pillow 输出干净无抗锯齿，最合适 |
| matplotlib | 统计图表 | ⚠️ 辅助用途：可生成"颜色分布直方图"，帮用户判断颜色用量；但用它直接画像素网格会有边缘抗锯齿/糊的问题，不作主渲染 |
| seaborn / plotly | 统计 / 交互图表 | ❌ 不适用，图纸不是图表 |
| reportlab | PDF | 后续迭代（M2）用于 PDF 排版导出 |

### 4.3 关键设计

- **颜色匹配**：RGB 转 **CIE Lab** 后取欧氏距离最近邻（比 RGB 距离更符合人眼感知），已实现（含同豆号合并、组内一致性拆分、色差 `!` 提示）；
- **透明处理**：RGBA 纹理先合成到白色背景（模拟实体豆盘）；
- **轴编号**：左侧行号 + 顶部列号，便于数豆；
- **网格线**：半透明黑色 1px 线，叠加在色块上，深浅色背景上都可见。

## 5. 子系统 B：MC 模组（Fabric）

> 26.2 属较新版本，开发前需确认对应的 Fabric Loader / Yarn mappings 版本；备选 NeoForge。

### 5.1 模块划分

| 模块 | 职责 | 关键实现思路 |
|------|------|--------------|
| `plane` | 轴对齐平面解析（两对角坐标） | 校验共面、推导法线与 U/V 轴、尺寸 = 边长差 +1；`view` 与 `section` 共用 |
| `section` / `view` | 截面 / 正交视图采样 | 双版本：**服务端版**（MapColor 简化，服务器环境可用）+ **客户端版 ClientPerlerCommand**（面纹理全功能，ClientLevel 采样） |
| `screenshot` (F3) | 玩家视角 → 图纸 | 客户端命令 + **F9 快捷键**（GLFW 轮询 + `mc.gui.screen()==null` 防误截）+ **配置命令**（properties 持久化） |
| `export` | 网格 → 契约 JSON | palette 按**豆号聚合**（代表色 + `bead_rgb` 豆号色字段）+ MardPalette 匹配 |
| `FaceTextureProvider` (M5) | 方块某面的 16×16 纹理像素 | **自解析 assets 模型**（blockstates variants → model parent 链/textures 变量 → face UV → 贴图），不依赖 26.2 渲染 API |
| `MardPalette` | Java 版 MARD 色卡匹配 | sRGB→Lab 最近邻，与 Python 端算法逐位一致（交叉验证 6/6） |

### 5.2 指令规范

```
/perler view <x1> <y1> <z1> <x2> <y2> <z2> --direction <east|south|west|north|up|down> [选项]
/perler section <x1> <y1> <z1> <x2> <y2> <z2> [选项]
/perler screenshot --long-edge N [选项]                  # 客户端命令, 取当前视角
```

| 选项 | 说明 | 默认 |
|------|------|------|
| `--distance N` | view 探测最大距离 | 16（**硬上限 256**，超限拒绝） |
| `--ignore <id>` | 追加到忽略集（可重复） | 忽略集 = `minecraft:air`, `minecraft:barrier`, `minecraft:structure_void` |
| `--keep <id>` | 从忽略集移除（如 `--keep minecraft:air` 看空气层） | 无 |
| `--granularity N` | 聚合粒度 1/2/4/8/16：1 颗豆 = 方块面纹理 N×N 像素的平均色（16=整面平均=1方块1豆） | 16 |
| `--long-edge N` | screenshot 专用：图纸长边豆数，短边按截图宽高比自动 | 64 |
| `--bg <r,g,b>` | 背景色 | 白 |
| `--palette <brand>` | 豆号色卡 | mard |
| `--scale N` | 单格像素（仅影响后续渲染元数据） | 64 |
| `--name <s>` | 图纸名（导出 JSON 的 `name`） | 自动（含坐标与方向） |
| `--save <path>` | 输出路径 | `.minecraft/patterns/<name>.json` |
| `--json` | 显式导出契约 JSON | on |

**方向合法性**（按平面法线校验，非法拒绝并提示合法方向）：

| 平面法线 | 合法方向 |
|----------|----------|
| X（x1==x2，竖直） | east / west |
| Z（z1==z2，竖直） | north / south |
| Y（y1==y2，水平） | up / down |

坐标系：north=-Z、south=+Z、east=+X、west=-X。

### 5.3 正交视图采样算法（view）

```
for 平面格子 (u,v):                       # 每格 = 1 个方块位置
  state = 沿视向探测第一个有效方块          # 忽略集/距离规则同前; 无 → 背景色
  if state 有效:
    tex = BlockColorMapper.faceTexture(state, direction)   # 被看到那一面的 16×16 纹理像素
    for 每个 g×g 像素块 (共 (16/g)² 块, g = --granularity):
      grid[对应豆格] = 块内像素平均色
```

- 语义：**正交投影 / 每方块光线投射**，1 方块 = (16/g)² 颗豆：
  - `--granularity 16`（默认）：整面平均 → 1 方块 = 1 豆；
  - `--granularity 8 / 4 / 2 / 1`：1 方块 = 2×2 / 4×4 / 8×8 / 16×16 豆（g=1 纹理原样，最精细）；
- `section` = 同一遍历但 d=0（取平面本身），不向前探测；面方向取 UP；
- **取色（M5 已实现）**：客户端版每格尝试 `FaceTextureProvider.getFacePixels(state, direction)` → 16×16 ARGB → 按 granularity 做 g×g 块平均（透明像素跳过）；解析失败回退 MapColor 单色；服务端版直接 MapColor；
- 输出豆数上限：面格数 × (16/g)² ≤ 1M 豆，超限拒绝（granularity=1 时 100×100 面 = 256 万豆，不允许）。

### 5.4 通用选项（贯穿）

- **忽略集**（view/section 共用）：默认 `air` / `barrier` / `structure_void` 视为背景，`--ignore` 追加、`--keep` 移除；
- **聚合粒度**（view/section 共用）：`--granularity 1|2|4|8|16` 控制 1 豆 = 面纹理内 g×g 像素；
- 半透明方块（玻璃）→ 可选的混合模式（直接取色 / 混合背景，后续迭代）；
- 背景色：`--bg <r,g,b>`。

### 5.5 性能与安全

- **异步执行**：服务端/客户端命令均在独立线程池运行，完成发回执（消息），不进主线程避免卡服/卡渲染；
- **硬上限**：面尺寸 ≤ 512×512、`--distance` ≤ 256、**输出豆数 ≤ 1M**（面格数 × (16/g)²），超限直接拒绝并提示；
- 大区域采样分块遍历 + 按 chunk 缓存 `getBlockState` 结果。

## 6. 关键决策记录

| 决策 | 理由 |
|------|------|
| F1 用独立 Python 脚本而非模组 | 离线可用、迭代快、无游戏依赖；用户确认后期模组内也可复用 |
| 1 方块 = 1 像素（view/section） | 直接映射，零插值，图纸与游戏一一对应 |
| view 用逐格光线投射（正交投影） | 天然处理非凸结构；语义直观（"向该方向看过去最近的方块"） |
| 探测距离硬上限 + 异步执行 | 防大面×长距离卡死；指令即发即回，完成回执 |
| 方向合法性按平面法线校验 | 竖直面上下无效、水平面东西南北无效，减少误用 |
| 优先取"被看到的那一面"的纹理 | 视图更真实（从北面看应是北面纹理而非整体平均色） |
| **面纹理用自解析 assets 模型**（M5） | 26.2 渲染 API 重构为渲染状态机，提取像素风险高；blockstates/models JSON 是稳定数据源，与 Python 端同源 |
| **section/view 双版本**（服务端 + 客户端） | 面纹理需要客户端资源（模型/贴图）；服务端版保留供服务器环境 |
| 截图用 F9 快捷键为主 | 按键瞬间无输入框/聊天栏，画面天然干净；命令保留为参数化入口 |
| 共享 JSON 图纸格式 | 模组与脚本互操作；Web 平台直接消费 |
| 墙面并入 view | 竖直面 + 水平方向 = 墙面视图，一个指令覆盖，不重复实现 |
| 截图用客户端命令 + 平均降采样 | 截图需在渲染线程执行；平均采样比最近邻平滑 |
| Web 平台手动保存 + 离屏 canvas/脏矩形 | 大图编辑性能关键；保存操作带视觉反馈（blur + 动画） |
| Pillow 作主渲染库 | 像素级控制、无抗锯齿干扰、跨平台 |
| 颜色匹配用 Lab 空间 | 符合人眼感知，匹配结果更"像" |

## 7. 测试与验证

- **PatternTool**：单元测试（网格构建、透明处理、坐标标注）+ 基准贴图输出快照人工比对；
- **模组**：
  - 方向合法性：6 种方向 × 3 种平面法线全组合，非法方向必须拒绝并提示；
  - 采样正确性：已知结构（含空气层、玻璃、深墙）的 view 输出与预期比对；
  - 边界：`--distance` 超上限、面超 512×512 必须拒绝；
  - 截图：不同分辨率降采样结果比对（通道序：26.2 `NativeImage.getPixel` 返回 **ARGB**，已实证修正）；
- **Web 平台**：vitest 单元测试（契约解析 / flood fill / bgColor 等 13 项）；`npm run build` 类型检查；256×256 图纸编辑性能基准（离屏 canvas + 脏矩形后拖拽流畅）。

## 8. 26.2 生态与构建经验（踩坑记录）

| 坑 | 结论 |
|----|------|
| Fabric Loom 版本 | 真实版本带补丁号（1.17.17）；26.x 需 `1.17-SNAPSHOT`，且 Fabric maven **无插件 marker** → 用 buildscript classpath 方式加载 |
| 26.1+ 不再混淆 | 无 yarn、无官方 mappings 文件（版本 JSON 只有 client/server）→ **不配置 mappings**，直接 `minecraft "com.mojang:minecraft:26.2"` |
| 无 remap 配置 | Loom `disableObfuscation()` 后不注册 modImplementation → 依赖用普通 `implementation`（官方模板同款） |
| brigadier 1.3.10 | `then(ArgumentBuilder)` **立即 build 快照**——先配置好子节点再挂链（坐标链反向构建，executes 挂链尾） |
| `Commands.literal/argument` | 26.2 **硬编码 CommandSourceStack**（非泛型）→ 客户端命令用纯 brigadier builder（`LiteralArgumentBuilder`/`RequiredArgumentBuilder`） |
| `NativeImage.getPixel` | 内部已 `ARGB.fromABGR()` 转换 → 返回 **ARGB**（不要按 ABGR 解析） |
| 当前屏幕 | `mc.screen` 字段移除 → **`mc.gui.screen()`** |
| 命令反馈 | `sendFeedback` → `sendSuccess` / `sendFailure`；权限 `hasPermission(int)` → `permissions().hasPermission(Permissions.COMMANDS_ADMIN)` |
| 属性访问 | `Property.getValue(state)` → **`state.getValue(Property)`**（方法移入 StateHolder/BlockState） |
| 资源类型 | `ResourceLocation` → **`Identifier`**（`Identifier.parse`）；`ResourceManager.getResource` → `getResourceStack`（返回 List）；`Resource` 是 record 非 AutoCloseable |
| Gradle 代理 | wrapper 下载发行版不走 gradle.properties → 用 `GRADLE_OPTS` 注入 JVM 代理参数 |
| Web 端 IndexedDB | Vue reactive Proxy 不可结构化克隆 → 写入前 JSON 深拷贝（`toPlain`） |
